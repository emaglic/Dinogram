import shapeSchema from "./shape.schema.json";
import shapeUISchema from "./shape.uischema.json";

const schemas = {
  shape: {
    schema: shapeSchema,
    uischema: shapeUISchema,
  },
};

export default schemas;
